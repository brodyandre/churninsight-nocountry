spring.application.name=churninsight-backend

# URL base do serviço Python/FastAPI
# Local (sem Docker): usa o default 127.0.0.1
# Docker/OCI/OKE: sobrescreve via DS_SERVICE_URL
ds.service.url=${DS_SERVICE_URL:http://127.0.0.1:8000}

# Porta do Spring Boot (UI + proxy)
server.port=8080
